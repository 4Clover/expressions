---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - turbo.json
  - apps/web/package.json
  - apps/web/svelte.config.js
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/app.html
  - apps/web/src/app.d.ts
  - apps/web/src/routes/+page.svelte
  - apps/web/src/routes/+layout.svelte
  - packages/config/tsconfig.base.json
autonomous: true

must_haves:
  truths:
    - "Developer can run `pnpm dev` from root and SvelteKit dev server starts"
    - "Turborepo caches builds correctly (second build is instant)"
    - "TypeScript compiles without errors across all workspaces"
  artifacts:
    - path: "turbo.json"
      provides: "Monorepo task configuration with SvelteKit outputs"
      contains: ".svelte-kit/cloudflare"
    - path: "apps/web/svelte.config.js"
      provides: "SvelteKit configuration with Cloudflare adapter"
      contains: "adapter-cloudflare"
    - path: "apps/web/vite.config.ts"
      provides: "Vite config with Tailwind v4 plugin"
      contains: "@tailwindcss/vite"
    - path: "packages/config/tsconfig.base.json"
      provides: "Shared TypeScript configuration"
  key_links:
    - from: "package.json"
      to: "turbo.json"
      via: "scripts calling turbo"
      pattern: "turbo (dev|build)"
    - from: "apps/web/package.json"
      to: "packages/config"
      via: "workspace dependency"
      pattern: "@repo/config"
---

<objective>
Set up the Turborepo monorepo structure with pnpm workspaces, SvelteKit 2 app, and Cloudflare adapter.

Purpose: Establish the foundation that all other plans build upon - the monorepo structure, build system, and base SvelteKit application.
Output: Working monorepo where `pnpm dev` starts the SvelteKit dev server and `pnpm build` produces cached builds.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

Reference the research document for exact package versions, Turborepo configuration patterns, and SvelteKit setup.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Turborepo and root package</name>
  <files>
    - package.json
    - turbo.json
    - packages/config/package.json
    - packages/config/tsconfig.base.json
  </files>
  <action>
Update root package.json:
- Add scripts: `"dev": "turbo dev"`, `"build": "turbo build"`, `"lint": "turbo lint"`, `"check": "turbo check"`
- Add devDependency: `turbo@^2.7.0`

Create turbo.json with task configuration:
- `build`: dependsOn `^build`, outputs `[".svelte-kit/**", ".svelte-kit/cloudflare/**"]`
- `dev`: cache false, persistent true
- `lint`: empty (parallel)
- `check`: dependsOn `^build`
- `db:generate` and `db:push`: cache false (for Phase 1 Plan 2)

Create packages/config/package.json:
- name: `@repo/config`
- exports: `{ "./tsconfig.base": "./tsconfig.base.json" }`

Create packages/config/tsconfig.base.json:
- Shared strict TypeScript config
- target ES2022, module ESNext, moduleResolution bundler
- strict: true, exactOptionalPropertyTypes: true
  </action>
  <verify>
- `cat turbo.json` shows correct task configuration
- `cat packages/config/tsconfig.base.json` shows strict config
  </verify>
  <done>
Root Turborepo configuration exists with correct outputs for SvelteKit/Cloudflare builds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SvelteKit app with Cloudflare adapter</name>
  <files>
    - apps/web/package.json
    - apps/web/svelte.config.js
    - apps/web/vite.config.ts
    - apps/web/tsconfig.json
    - apps/web/src/app.html
    - apps/web/src/app.d.ts
    - apps/web/src/app.css
    - apps/web/src/routes/+page.svelte
    - apps/web/src/routes/+layout.svelte
  </files>
  <action>
Create apps/web as a SvelteKit 2 application:

1. Create apps/web/package.json:
   - name: `@repo/web`
   - type: `module`
   - scripts: dev, build, preview, check, lint
   - dependencies: `@sveltejs/kit`, `svelte`
   - devDependencies: `@sveltejs/adapter-cloudflare`, `@sveltejs/vite-plugin-svelte`, `@tailwindcss/vite`, `tailwindcss@^4`, `typescript`, `vite`
   - Reference `@repo/config` via workspace protocol

2. Create svelte.config.js:
   - Import adapter-cloudflare
   - Use vitePreprocess
   - Configure routes include/exclude per research

3. Create vite.config.ts:
   - Import @tailwindcss/vite plugin (BEFORE sveltekit - order matters)
   - Import sveltekit plugin
   - Configure as defineConfig

4. Create tsconfig.json:
   - Extend @repo/config/tsconfig.base
   - Include `.svelte-kit` paths

5. Create src/app.html:
   - Standard SvelteKit HTML template with %sveltekit.head% and %sveltekit.body%

6. Create src/app.d.ts:
   - App namespace with Platform interface for Cloudflare bindings (empty for now)
   - PageData, Error, Locals interfaces

7. Create src/app.css:
   - Import tailwindcss
   - Placeholder for design tokens (Plan 3 will populate)

8. Create src/routes/+layout.svelte:
   - Import app.css
   - Render @render children()

9. Create src/routes/+page.svelte:
   - Simple "Expressions Hair Designs" heading
   - Placeholder text confirming setup works
  </action>
  <verify>
- `pnpm install` completes without errors
- `pnpm dev` starts dev server on localhost:5173
- `pnpm build` completes and outputs to .svelte-kit/cloudflare
- `pnpm check` reports no TypeScript errors
  </verify>
  <done>
SvelteKit app runs in dev mode, builds for Cloudflare, and TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. From project root: `pnpm install` succeeds
2. From project root: `pnpm dev` starts SvelteKit dev server
3. From project root: `pnpm build` completes (verify second run is cached)
4. From project root: `pnpm check` reports no TypeScript errors
5. Visit http://localhost:5173 - see placeholder page
</verification>

<success_criteria>
- `pnpm dev` starts SvelteKit dev server from project root
- `pnpm build` produces .svelte-kit/cloudflare output
- Second `pnpm build` is instant (Turborepo cache hit)
- TypeScript compiles without errors
- Monorepo structure matches research architecture (apps/web, packages/config)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
