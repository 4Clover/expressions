---
phase: 02-staff-services-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/staff.ts
  - packages/db/src/schema/services.ts
  - packages/db/src/schema/index.ts
  - apps/web/src/lib/components/staff/StaffCard.svelte
  - apps/web/src/lib/components/staff/StaffProfile.svelte
  - apps/web/src/lib/components/services/ServiceCard.svelte
  - apps/web/src/lib/components/services/ServiceCategory.svelte
  - apps/web/src/lib/components/services/PriceDisplay.svelte
  - apps/web/src/routes/staff/+page.svelte
  - apps/web/src/routes/staff/+page.server.ts
  - apps/web/src/routes/staff/[slug]/+page.svelte
  - apps/web/src/routes/staff/[slug]/+page.server.ts
  - apps/web/src/routes/services/+page.svelte
  - apps/web/src/routes/services/+page.server.ts
autonomous: true

must_haves:
  truths:
    - "Customer can view list of active stylists with photos and specialties"
    - "Customer can view individual stylist profile page with bio and services"
    - "Customer can browse services organized by category"
    - "Services display correct price format (fixed, starting, or range)"
    - "Per-stylist pricing shows when different from base service price"
  artifacts:
    - path: "packages/db/src/schema/staff.ts"
      provides: "Drizzle relations for staff queries"
      contains: "relations"
    - path: "packages/db/src/schema/services.ts"
      provides: "Drizzle relations for services queries"
      contains: "relations"
    - path: "apps/web/src/routes/staff/+page.svelte"
      provides: "Staff listing page"
      min_lines: 20
    - path: "apps/web/src/routes/staff/[slug]/+page.svelte"
      provides: "Individual staff profile page"
      min_lines: 40
    - path: "apps/web/src/routes/services/+page.svelte"
      provides: "Service catalog page"
      min_lines: 30
  key_links:
    - from: "apps/web/src/routes/staff/+page.server.ts"
      to: "@repo/db"
      via: "import and db.query.staff"
      pattern: "db\\.query\\.staff"
    - from: "apps/web/src/routes/services/+page.server.ts"
      to: "@repo/db"
      via: "import and db.query.serviceCategories"
      pattern: "db\\.query\\.(services|serviceCategories)"
---

<objective>
Build staff and services display pages with database-driven content.

Purpose: Create the operational data display that booking flow will use — staff profiles and service catalog with per-stylist pricing.
Output: Staff listing page, individual staff profiles, service catalog organized by category, all pulling from the Phase 1 database schema.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-staff-services-content/02-RESEARCH.md

# Phase 1 provides database schema and design system
@packages/db/src/schema/staff.ts
@packages/db/src/schema/services.ts
@apps/web/src/lib/components/ui/button/button.svelte
@apps/web/src/lib/components/ui/card/card.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Drizzle relations and public RLS policies</name>
  <files>
    packages/db/src/schema/staff.ts
    packages/db/src/schema/services.ts
    packages/db/src/schema/index.ts
  </files>
  <action>
Add Drizzle relations v2 to enable eager loading:

1. In staff.ts:
   - Import `relations` from 'drizzle-orm'
   - Import `profiles` from './profiles.js'
   - Add `staffRelations = relations(staff, ({ one, many }) => ({ ... }))`
   - Relate to profiles (one) and staffServices (many)

2. In services.ts:
   - Add relations for serviceCategories -> services (one-to-many)
   - Add relations for services -> staffServices (one-to-many)
   - Add relations for staffServices -> staff and service (many-to-one each)

3. In index.ts:
   - Export all relations alongside tables

4. Add RLS policies for public read access:
   - Import `pgPolicy` and `sql` from 'drizzle-orm/pg-core'
   - Import `anonRole, authenticatedRole` from 'drizzle-orm/supabase'
   - Add policy to staff: "public can view active staff" for SELECT where isActive = true
   - Add policy to serviceCategories: "public can view categories" for SELECT (all rows)
   - Add policy to services: "public can view active services" for SELECT where isActive = true
   - Add policy to staffServices: "public can view available staff services" for SELECT where isAvailable = true

Note: Drizzle relations v2 uses the pattern shown in research. Relations enable `db.query.staff.findMany({ with: { staffServices: { with: { service: true }}}})`.
  </action>
  <verify>
    - pnpm check passes with 0 errors
    - Relations are exported from @repo/db/schema
    - Run `pnpm --filter @repo/db db:generate` to verify schema compiles
  </verify>
  <done>
    - Drizzle relations defined for all staff/services tables
    - RLS policies allow anonymous read of active staff/services
    - Schema exports relations for eager loading
  </done>
</task>

<task type="auto">
  <name>Task 2: Create staff and service display components</name>
  <files>
    apps/web/src/lib/components/staff/StaffCard.svelte
    apps/web/src/lib/components/services/ServiceCard.svelte
    apps/web/src/lib/components/services/ServiceCategory.svelte
    apps/web/src/lib/components/services/PriceDisplay.svelte
  </files>
  <action>
Create reusable display components using existing Card component:

1. StaffCard.svelte:
   - Props: staff object with displayName, photoUrl, bio, specialties[]
   - Display photo (or placeholder), name, truncated bio (line-clamp-2), specialties as badges
   - Link to /staff/[slug] using slugified displayName
   - Use Card component, apply shadow + hover:shadow-lg transition

2. PriceDisplay.svelte:
   - Props: priceType ('fixed'|'starting'|'range'), priceMin (cents), priceMax? (cents)
   - Helper function: formatPrice(cents) returns "$XX" (divide by 100, no decimals)
   - Display logic: fixed = "$45", starting = "Starting at $65", range = "$45 - $65"
   - Use $derived for displayPrice computation

3. ServiceCard.svelte:
   - Props: service object with name, description, durationMinutes, priceType, priceMin, priceMax
   - Display name, description (line-clamp-2), duration (e.g., "45 min"), PriceDisplay
   - Use Card component with hover effect

4. ServiceCategory.svelte:
   - Props: category object with name, services array
   - Display category name as heading (h2)
   - Map services to ServiceCard in grid layout (1 col mobile, 2 cols md, 3 cols lg)

Use Svelte 5 $props() syntax. Import cn() from '$lib/utils' for class merging.
  </action>
  <verify>
    - pnpm check passes
    - Components export from index.ts files
    - No TypeScript errors in component files
  </verify>
  <done>
    - StaffCard displays staff member with link to profile
    - PriceDisplay handles all three price types correctly
    - ServiceCard shows service with duration and price
    - ServiceCategory groups services under heading
  </done>
</task>

<task type="auto">
  <name>Task 3: Create staff and services pages with server data loading</name>
  <files>
    apps/web/src/routes/staff/+page.svelte
    apps/web/src/routes/staff/+page.server.ts
    apps/web/src/routes/staff/[slug]/+page.svelte
    apps/web/src/routes/staff/[slug]/+page.server.ts
    apps/web/src/routes/services/+page.svelte
    apps/web/src/routes/services/+page.server.ts
    apps/web/src/lib/components/staff/StaffProfile.svelte
  </files>
  <action>
Create route pages that load from database:

1. /staff/+page.server.ts:
   - Import db from @repo/db, staff from @repo/db/schema
   - Query: db.query.staff.findMany({ where: eq(staff.isActive, true) })
   - Return { staff: staffMembers }
   - Handle missing DATABASE_URL gracefully (return empty array)

2. /staff/+page.svelte:
   - Grid of StaffCard components (1/2/3 columns responsive)
   - Page title "Our Stylists" with Cormorant Garamond heading
   - Empty state if no staff

3. /staff/[slug]/+page.server.ts:
   - Import eq, and from 'drizzle-orm'
   - Convert slug to displayName: params.slug.split('-').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' ')
   - Query with relations: db.query.staff.findFirst({ where: and(eq(staff.displayName, displayName), eq(staff.isActive, true)), with: { staffServices: { where: eq(staffServices.isAvailable, true), with: { service: { with: { category: true } } } } } })
   - Return 404 if not found using error() from @sveltejs/kit
   - Return { staffMember }

4. /staff/[slug]/+page.svelte:
   - Use StaffProfile component (create it) for layout
   - Full photo, name, bio, specialties list
   - Section: "Services by [Name]" listing their available services with custom pricing

5. StaffProfile.svelte:
   - Props: staffMember with nested staffServices
   - Two-column layout on desktop (photo left, details right)
   - Display custom prices if set, else service base price
   - Use PriceDisplay for pricing

6. /services/+page.server.ts:
   - Query: db.query.serviceCategories.findMany({ with: { services: { where: eq(services.isActive, true) } }, orderBy: [asc(serviceCategories.displayOrder)] })
   - Return { categories }

7. /services/+page.svelte:
   - Page title "Our Services"
   - Map categories to ServiceCategory components
   - Each category renders its services in grid

All pages use design tokens from app.css (quiet luxury aesthetic).
  </action>
  <verify>
    - pnpm dev starts without errors
    - Navigate to /staff - shows staff listing (or empty state)
    - Navigate to /services - shows service categories (or empty state)
    - pnpm check passes
  </verify>
  <done>
    - Staff listing page displays active stylists
    - Individual staff profile shows bio, photo, and their services
    - Service catalog groups services by category
    - Price display shows correct format for each price type
    - All queries use Drizzle relations for eager loading
  </done>
</task>

</tasks>

<verification>
Phase verification after all tasks complete:

1. Database:
   - Relations enable eager loading queries
   - RLS policies ready for Supabase (will work after db:push)

2. Staff pages:
   - /staff shows grid of stylists
   - /staff/[slug] shows individual profile with services
   - Graceful handling when database not configured

3. Services page:
   - /services shows categories with services
   - Prices display correctly for all three price types
   - Duration shown for each service

4. Design consistency:
   - Uses established design tokens
   - Cards have shadow + hover effect
   - Headings use Cormorant Garamond
</verification>

<success_criteria>
- Staff listing page renders with StaffCard components
- Staff profile page shows full bio and services offered
- Services page organizes services by category
- PriceDisplay correctly handles fixed, starting, and range prices
- All pages gracefully handle empty database state
- pnpm check passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-staff-services-content/02-01-SUMMARY.md`
</output>
