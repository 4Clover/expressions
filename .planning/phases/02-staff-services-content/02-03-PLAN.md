---
phase: 02-staff-services-content
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - apps/web/src/routes/+page.svelte
  - apps/web/src/routes/+page.ts
  - apps/web/src/routes/about/+page.svelte
  - apps/web/src/routes/about/+page.ts
  - apps/web/src/routes/contact/+page.svelte
  - apps/web/src/routes/contact/+page.ts
  - apps/web/src/lib/components/storyblok/StaffHighlights.svelte
autonomous: true

must_haves:
  truths:
    - "Homepage displays hero section with headline, subheadline, and CTA"
    - "Homepage shows services overview section with category previews"
    - "Homepage features staff highlights with real stylist data from database"
    - "About page renders CMS content with salon story and location info"
    - "Contact page shows address, phone, hours, and embedded Google Map"
    - "All CMS pages support live editing via Storyblok Visual Editor"
  artifacts:
    - path: "apps/web/src/routes/+page.svelte"
      provides: "CMS-driven homepage"
      contains: "StoryblokComponent"
    - path: "apps/web/src/routes/+page.ts"
      provides: "Homepage CMS data fetch"
      contains: "cdn/stories/home"
    - path: "apps/web/src/routes/about/+page.svelte"
      provides: "About page with CMS content"
      contains: "StoryblokComponent"
    - path: "apps/web/src/routes/contact/+page.svelte"
      provides: "Contact page with CMS content"
      contains: "StoryblokComponent"
  key_links:
    - from: "apps/web/src/routes/+page.ts"
      to: "@storyblok/svelte"
      via: "storyblokApi.get"
      pattern: "storyblokApi\\.get"
    - from: "apps/web/src/lib/components/storyblok/StaffHighlights.svelte"
      to: "@repo/db"
      via: "fetch from server endpoint"
      pattern: "fetch.*api/staff"
---

<objective>
Create CMS-driven marketing pages that owners can edit via Storyblok.

Purpose: Deliver the homepage, about page, and contact page that showcase the salon and drive bookings. These pages use Storyblok content blocks (from Plan 02-02) and integrate with database staff data.
Output: Three marketing pages (/, /about, /contact) rendering CMS content with visual editor support.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-staff-services-content/02-RESEARCH.md

# Plan 02-01 creates staff/services pages and components
# Plan 02-02 creates Storyblok SDK and block components

# Storyblok block components (from 02-02)
@apps/web/src/lib/components/storyblok/Hero.svelte
@apps/web/src/lib/components/storyblok/ServicesOverview.svelte
@apps/web/src/lib/components/storyblok/StaffHighlights.svelte
@apps/web/src/lib/components/storyblok/ContactInfo.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CMS-driven homepage</name>
  <files>
    apps/web/src/routes/+page.ts
    apps/web/src/routes/+page.svelte
  </files>
  <action>
Replace the placeholder landing page with CMS-driven homepage:

1. Create src/routes/+page.ts:
   - Import type PageLoad from './$types'
   - Export load function that fetches Storyblok story
   - Get storyblokApi from parent() (provided by +layout.ts)
   - Fetch: storyblokApi.get('cdn/stories/home', { version: import.meta.env.DEV ? 'draft' : 'published' })
   - Return { story: response.data.story }
   - Handle error case: if story doesn't exist, return null and let page show fallback

2. Update src/routes/+page.svelte:
   - Import { StoryblokComponent, useStoryblokBridge } from '@storyblok/svelte'
   - Import { browser } from '$app/environment'
   - Accept data via `let { data } = $props()`
   - Create reactive story state: `let story = $state(data.story)`
   - Use $effect to setup bridge only in browser:
     ```
     $effect(() => {
       if (browser && story) {
         useStoryblokBridge(story.id, (newStory) => (story = newStory));
       }
     });
     ```
   - Render: `{#if story}<StoryblokComponent blok={story.content} />{:else}...fallback...{/if}`
   - Fallback shows existing styled landing page (graceful degradation when CMS not configured)

3. Keep existing landing page content as fallback:
   - Hero section with "Expressions Hair Designs"
   - Service highlight cards
   - This ensures site works even without Storyblok configured

Note: The homepage will render Hero, ServicesOverview, StaffHighlights blocks as configured in Storyblok.
  </action>
  <verify>
    - pnpm check passes
    - pnpm dev shows homepage (fallback if no Storyblok configured)
    - No TypeScript errors in +page.ts or +page.svelte
  </verify>
  <done>
    - Homepage fetches story from Storyblok 'home' slug
    - StoryblokBridge enables live editing
    - Fallback content displays when CMS not configured
    - Ready for CMS content once Storyblok space is set up
  </done>
</task>

<task type="auto">
  <name>Task 2: Create about and contact pages</name>
  <files>
    apps/web/src/routes/about/+page.ts
    apps/web/src/routes/about/+page.svelte
    apps/web/src/routes/contact/+page.ts
    apps/web/src/routes/contact/+page.svelte
  </files>
  <action>
Create about and contact pages following same CMS pattern:

1. /about/+page.ts:
   - Same pattern as homepage
   - Fetch: storyblokApi.get('cdn/stories/about', { version: ... })
   - Return { story } or null for fallback

2. /about/+page.svelte:
   - Same StoryblokComponent + bridge pattern
   - Fallback: Static about page content
     - Heading "About Expressions"
     - Placeholder paragraph about salon history
     - Image placeholder area
   - SEO: Add svelte:head with title "About | Expressions Hair Designs"

3. /contact/+page.ts:
   - Fetch: storyblokApi.get('cdn/stories/contact', { version: ... })
   - Return { story } or null

4. /contact/+page.svelte:
   - Same pattern
   - Fallback: Static contact info
     - Heading "Contact Us"
     - Address placeholder
     - Phone placeholder
     - Hours placeholder (formatted list)
     - Map iframe placeholder (empty box with "Map will appear here")
   - SEO: Add svelte:head with title "Contact | Expressions Hair Designs"

Both pages use the Page block component which renders nested blocks (RichTextBlock, ContactInfo, etc.) from Storyblok.
  </action>
  <verify>
    - pnpm dev: Navigate to /about - shows fallback content
    - pnpm dev: Navigate to /contact - shows fallback content
    - pnpm check passes
    - Both pages have proper svelte:head titles
  </verify>
  <done>
    - About page renders CMS content or fallback
    - Contact page renders CMS content or fallback
    - Both support Storyblok visual editor
    - SEO titles set for each page
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire StaffHighlights to fetch real staff data</name>
  <files>
    apps/web/src/lib/components/storyblok/StaffHighlights.svelte
    apps/web/src/routes/api/staff/+server.ts
  </files>
  <action>
Update StaffHighlights block to display real staff from database:

1. Create API endpoint src/routes/api/staff/+server.ts:
   - Export GET handler
   - Import db from @repo/db, staff from @repo/db/schema
   - Import { eq } from 'drizzle-orm'
   - Query: db.query.staff.findMany({ where: eq(staff.isActive, true) })
   - Handle missing DATABASE_URL gracefully (return empty array)
   - Return JSON response: json({ staff: staffMembers })

2. Update StaffHighlights.svelte:
   - Add $state for staffMembers: `let staffMembers = $state<StaffMember[]>([])`
   - Use $effect to fetch staff on mount:
     ```
     $effect(() => {
       fetch('/api/staff')
         .then(r => r.json())
         .then(data => { staffMembers = data.staff })
         .catch(() => { staffMembers = [] });
     });
     ```
   - Define StaffMember type: { id, displayName, photoUrl, bio, specialties }
   - If blok.staff_ids is populated (from CMS), filter to only show those IDs
   - If blok.staff_ids is empty, show all active staff
   - Display staff in 3-column grid using existing StaffCard component pattern:
     - Photo (or placeholder)
     - Name
     - Specialties as badges
     - Link to /staff/[slug]
   - Keep storyblokEditable on root element

3. Helper function for slug:
   ```typescript
   function toSlug(name: string): string {
     return name.toLowerCase().replace(/\s+/g, '-');
   }
   ```

This bridges CMS-driven homepage with database staff data, fulfilling requirement CONT-01 (staff highlights on homepage).
  </action>
  <verify>
    - pnpm check passes
    - API endpoint: curl https://localhost:5173/api/staff returns JSON (or empty array)
    - StaffHighlights renders staff cards when data available
    - Links to /staff/[slug] are correct format
  </verify>
  <done>
    - API endpoint returns active staff from database
    - StaffHighlights displays real staff data
    - CMS can optionally filter to specific staff_ids
    - Staff cards link to profile pages
  </done>
</task>

</tasks>

<verification>
Phase verification after all tasks complete:

1. Homepage (/):
   - Fetches 'home' story from Storyblok
   - Renders CMS blocks (Hero, ServicesOverview, StaffHighlights)
   - Shows fallback when CMS not configured
   - Live editing works via Storyblok bridge

2. About Page (/about):
   - Fetches 'about' story from Storyblok
   - Renders CMS content or fallback
   - Has SEO title

3. Contact Page (/contact):
   - Fetches 'contact' story from Storyblok
   - Renders contact info with map area
   - Has SEO title

4. StaffHighlights Integration:
   - API endpoint returns real staff data
   - Component displays staff from database
   - Optional filtering by CMS-configured staff_ids
</verification>

<success_criteria>
- Homepage renders Hero, ServicesOverview, StaffHighlights (from CMS or fallback)
- About page displays salon story content
- Contact page displays contact info and hours
- StaffHighlights shows real staff from database
- All pages support Storyblok visual editor live preview
- pnpm check passes with 0 errors
- Navigation between pages works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-staff-services-content/02-03-SUMMARY.md`
</output>
