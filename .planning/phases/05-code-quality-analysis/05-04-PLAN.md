---
phase: 05-code-quality-analysis
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/hooks.server.ts
  - apps/web/src/lib/components/booking/types.ts
  - apps/web/src/lib/components/booking/index.ts
  - apps/web/src/lib/components/booking/SlotGrid.svelte
  - apps/web/src/lib/components/booking/DateTimeStep.svelte
  - apps/web/src/lib/components/gallery/types.ts
  - apps/web/src/lib/components/gallery/index.ts
  - apps/web/src/lib/components/gallery/GalleryGrid.svelte
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "tsc --noEmit passes with zero errors"
    - "svelte-check continues to pass with zero errors"
    - "All TypeScript types are properly annotated"
  artifacts:
    - path: "apps/web/src/hooks.server.ts"
      provides: "Typed SvelteKit handle function"
      contains: "Handle"
    - path: "apps/web/src/lib/components/booking/types.ts"
      provides: "TimeSlot type definition"
      exports: ["TimeSlot"]
    - path: "apps/web/src/lib/components/gallery/types.ts"
      provides: "GalleryItem type definition"
      exports: ["GalleryItem"]
  key_links:
    - from: "apps/web/src/lib/components/booking/index.ts"
      to: "apps/web/src/lib/components/booking/types.ts"
      via: "type re-export"
      pattern: "export type \\{ TimeSlot \\} from './types'"
    - from: "apps/web/src/lib/components/gallery/index.ts"
      to: "apps/web/src/lib/components/gallery/types.ts"
      via: "type re-export"
      pattern: "export type \\{ GalleryItem \\} from './types'"
---

<objective>
Fix TypeScript errors found during verification to achieve zero tsc errors.

Purpose: Close the gap identified in 05-VERIFICATION.md where 4 TypeScript errors prevent the phase goal of "zero TypeScript type errors in strict mode" from being achieved.

Output: All TypeScript errors resolved, tsc --noEmit passes cleanly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-code-quality-analysis/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Handle type to hooks.server.ts</name>
  <files>apps/web/src/hooks.server.ts</files>
  <action>
Import the `Handle` type from `@sveltejs/kit` and apply it to the handle function.

Current code (line 30):
```typescript
export const handle = async ({ event, resolve }) => {
```

Change to:
```typescript
import type { Handle } from '@sveltejs/kit';

export const handle: Handle = async ({ event, resolve }) => {
```

This provides proper typing for the `event` (RequestEvent) and `resolve` (function) parameters without needing to annotate them individually.
  </action>
  <verify>Run `tsc --noEmit` in apps/web - no errors from hooks.server.ts</verify>
  <done>hooks.server.ts has explicit Handle type annotation, implicit any errors resolved</done>
</task>

<task type="auto">
  <name>Task 2: Extract TimeSlot type to types.ts</name>
  <files>
    - apps/web/src/lib/components/booking/types.ts (new)
    - apps/web/src/lib/components/booking/SlotGrid.svelte
    - apps/web/src/lib/components/booking/DateTimeStep.svelte
    - apps/web/src/lib/components/booking/index.ts
  </files>
  <action>
1. Create `apps/web/src/lib/components/booking/types.ts`:
```typescript
export interface TimeSlot {
  start: Date;
  end: Date;
  display: string;
}
```

2. Update `SlotGrid.svelte` - remove the type definition and import from types.ts:
- Remove lines 13-17 (the `export interface TimeSlot` block)
- Add import at top of script: `import type { TimeSlot } from './types';`

3. Update `DateTimeStep.svelte` - change import to use types.ts:
- Change: `import SlotGrid, { type TimeSlot } from "./SlotGrid.svelte";`
- To: `import SlotGrid from "./SlotGrid.svelte"; import type { TimeSlot } from "./types";`

4. Update `index.ts` - change re-export to use types.ts:
- Change line 14: `export type { TimeSlot } from './SlotGrid.svelte';`
- To: `export type { TimeSlot } from './types';`
  </action>
  <verify>Run `tsc --noEmit` - no errors about TimeSlot export from .svelte file</verify>
  <done>TimeSlot type lives in types.ts, all imports updated, barrel export works</done>
</task>

<task type="auto">
  <name>Task 3: Extract GalleryItem type to types.ts</name>
  <files>
    - apps/web/src/lib/components/gallery/types.ts (new)
    - apps/web/src/lib/components/gallery/GalleryGrid.svelte
    - apps/web/src/lib/components/gallery/index.ts
  </files>
  <action>
1. Create `apps/web/src/lib/components/gallery/types.ts`:
```typescript
export type GalleryItem = {
  id: string;
  type: "comparison" | "single";
  beforeImage?: string;
  afterImage?: string;
  image?: string;
  alt: string;
  stylistId?: string;
  categoryId?: string;
  caption?: string;
};
```

2. Update `GalleryGrid.svelte` - remove the type definition and import from types.ts:
- Remove lines 5-15 (the `export type GalleryItem` block)
- Add import at top of script: `import type { GalleryItem } from './types';`

3. Update `index.ts` - change re-export to use types.ts:
- Change line 5: `export type { GalleryItem } from "./GalleryGrid.svelte";`
- To: `export type { GalleryItem } from "./types";`
  </action>
  <verify>Run `tsc --noEmit` - no errors about GalleryItem export from .svelte file</verify>
  <done>GalleryItem type lives in types.ts, all imports updated, barrel export works</done>
</task>

</tasks>

<verification>
Run full TypeScript check from apps/web directory:
```bash
cd apps/web && npx tsc --noEmit
```

Expected output: No errors (exit code 0, no output)

Additionally verify svelte-check still passes:
```bash
cd apps/web && pnpm svelte-check
```

Expected: "svelte-check found 0 errors and 0 warnings"
</verification>

<success_criteria>
1. `tsc --noEmit` returns exit code 0 with no error output
2. `svelte-check` reports 0 errors and 0 warnings
3. All 4 TypeScript errors from VERIFICATION.md are resolved:
   - hooks.server.ts implicit any (fixed via Handle type)
   - TimeSlot export from .svelte (fixed via types.ts extraction)
   - GalleryItem export from .svelte (fixed via types.ts extraction)
4. Phase 5 success criterion "Zero TypeScript type errors in strict mode" is now met
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality-analysis/05-04-SUMMARY.md`
</output>
