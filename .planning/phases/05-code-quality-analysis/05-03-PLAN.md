---
phase: 05-code-quality-analysis
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .claude/get-shit-done/workflows/execute-plan.md
  - .claude/get-shit-done/agents/planner.md
  - .serena/memories/**/*.md
autonomous: true

must_haves:
  truths:
    - "Pattern documentation exists for future Claude reference"
    - "GSD executor workflow mandates Serena/Ref usage"
    - "GSD planner workflow mandates Serena/Ref usage"
    - "Todo created for remaining GSD workflow updates"
  artifacts:
    - path: ".serena/memories/patterns/*.md"
      provides: "Quick-reference patterns for Svelte 5, SvelteKit, Drizzle"
    - path: ".claude/get-shit-done/workflows/execute-plan.md"
      provides: "Modified executor with Serena/Ref mandate"
    - path: ".claude/get-shit-done/agents/planner.md"
      provides: "Modified planner with Serena/Ref mandate"
  key_links:
    - from: ".claude/get-shit-done/workflows/execute-plan.md"
      to: "Serena MCP"
      via: "tool usage instructions"
      pattern: "serena|Serena"
---

<objective>
Document established patterns for future development and modify GSD workflows to mandate Serena/Ref MCP usage.

Purpose: Per CONTEXT.md, this phase must "produce a plan to modify executor + planner (minimum) workflows to mandate Serena/Ref usage". Pattern documentation ensures consistent code quality in Phases 6-8.

Output: Pattern documentation in Serena memories, modified GSD workflows with Serena/Ref mandate, todo for remaining workflow updates.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-code-quality-analysis/05-RESEARCH.md
@.planning/phases/05-code-quality-analysis/05-CONTEXT.md
@.planning/phases/05-code-quality-analysis/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document patterns in Serena memories</name>
  <files>.serena/memories/patterns/*.md</files>
  <action>
Create pattern documentation for future Claude reference. Store in Serena memories for cross-session persistence.

**Create `.serena/memories/patterns/svelte5-patterns.md`:**
```markdown
# Svelte 5 Patterns - Quick Reference

## Props
```typescript
// CORRECT - Svelte 5
interface Props {
  name: string;
  optional?: number;
  bindable?: string;
  class?: string;
}
let { name, optional = 42, bindable = $bindable(), class: className }: Props = $props();

// WRONG - Svelte 4 (deprecated)
export let name: string;
```

## Reactive Derivations
```typescript
// CORRECT
let doubled = $derived(count * 2);
let complex = $derived.by(() => { /* multi-line */ return result; });

// WRONG - captures initial value only
const doubled = count * 2;
```

## Event Handlers
```typescript
// CORRECT - Svelte 5
<button onclick={handleClick}>

// WRONG - Svelte 4
<button on:click={handleClick}>
```

## Slots/Children
```svelte
<!-- CORRECT - Svelte 5 -->
{@render children?.()}

<!-- WRONG - Svelte 4 -->
<slot />
```
```

**Create `.serena/memories/patterns/sveltekit-patterns.md`:**
```markdown
# SvelteKit Patterns - Quick Reference

## Load Functions
```typescript
import type { PageServerLoad, Actions } from './$types';

export const load: PageServerLoad = async ({ params }) => {
  return { item: await fetchItem(params.id) };
};

export const actions = {
  default: async ({ request }) => {
    return fail(400, { error: 'message' });
  }
} satisfies Actions;
```

## API Routes
```typescript
import { json } from '@sveltejs/kit';
import type { RequestHandler } from './$types';

export const GET: RequestHandler = async ({ params }) => {
  return json({ data: result });
};
```
```

**Create `.serena/memories/patterns/drizzle-patterns.md`:**
```markdown
# Drizzle ORM Patterns - Quick Reference

## Queries
```typescript
import { db, users } from '@repo/db';
import { eq, and } from 'drizzle-orm';

// Type-safe query
const user = await db.query.users.findFirst({
  where: eq(users.id, userId),
});

// ALWAYS handle undefined (noUncheckedIndexedAccess)
if (!user) {
  return json({ error: 'Not found' }, { status: 404 });
}
```

## Array Access
```typescript
// WRONG - fails with noUncheckedIndexedAccess
const first = items[0].name;

// CORRECT - handle undefined
const first = items[0];
if (first) { first.name; }

// OR with nullish coalescing
const value = items[0] ?? defaultValue;
```
```
  </action>
  <verify>
- `ls .serena/memories/patterns/` shows svelte5-patterns.md, sveltekit-patterns.md, drizzle-patterns.md
- Each file has substantive content (>20 lines)
  </verify>
  <done>Pattern documentation created in Serena memories for future reference</done>
</task>

<task type="auto">
  <name>Task 2: Modify GSD executor workflow for Serena/Ref mandate</name>
  <files>.claude/get-shit-done/workflows/execute-plan.md</files>
  <action>
Read `.claude/get-shit-done/workflows/execute-plan.md` and add Serena/Ref mandate section.

**Add to the execution flow, near the top (after imports/context but before task execution):**

```markdown
## Tool Usage Requirements

**Serena MCP (repo navigation):**
- Use for ALL file search operations (search_codebase, list_files)
- Use for code edits when modifying existing files
- Use for dependency scanning (find_usages, find_references)

**Ref MCP / Context7 (documentation):**
- Use for verifying API patterns against official docs
- Use before implementing unfamiliar library patterns
- Use when resolving type errors or deprecation warnings
- Required libraries to verify: Svelte 5, SvelteKit 2, Drizzle ORM, Tailwind v4, shadcn-svelte

**Priority:**
1. Serena for codebase traversal (NOT grep/find)
2. Ref/Context7 for documentation lookup (NOT web search)
3. Standard tools only when MCP tools unavailable
```

Preserve all existing content - this is an addition, not a replacement.
  </action>
  <verify>
- `grep -c "Serena" .claude/get-shit-done/workflows/execute-plan.md` returns > 0
- `grep -c "Ref MCP\|Context7" .claude/get-shit-done/workflows/execute-plan.md` returns > 0
- File still contains original execution flow sections
  </verify>
  <done>Execute-plan workflow updated with Serena/Ref mandate</done>
</task>

<task type="auto">
  <name>Task 3: Modify GSD planner agent for Serena/Ref mandate and create todo</name>
  <files>.claude/get-shit-done/agents/planner.md, .planning/todos/pending/*.md</files>
  <action>
**Part 1: Update planner agent**

Read `.claude/get-shit-done/agents/planner.md` and add Serena/Ref guidance.

**Add to the discovery/research section:**

```markdown
## Tool Usage for Planning

**When analyzing codebase:**
- Use Serena MCP for file discovery and dependency analysis
- Prefer `search_codebase` over glob patterns for semantic search
- Use `find_usages` to understand impact of changes

**When planning implementations:**
- Use Ref MCP / Context7 to verify library patterns before including in plans
- Cross-reference RESEARCH.md recommendations with current docs
- Flag if docs have changed since research date
```

**Part 2: Create todo for remaining workflows**

Create `.planning/todos/pending/2026-01-21-gsd-workflow-updates.md`:
```markdown
# GSD Workflow Updates - Serena/Ref Integration

**Created:** 2026-01-21
**Priority:** Medium
**Category:** tooling

## Context

Phase 5 (Code-Quality-Analysis) updated executor and planner workflows to mandate Serena/Ref MCP usage. Remaining GSD workflows should also be updated for consistency.

## Remaining Workflows

1. `.claude/get-shit-done/workflows/research.md`
2. `.claude/get-shit-done/workflows/verify-phase.md`
3. `.claude/get-shit-done/workflows/execute-phase.md`
4. `.claude/get-shit-done/agents/checker.md`
5. `.claude/get-shit-done/agents/researcher.md`

## Action Items

- [ ] Add Serena mandate for codebase navigation in each
- [ ] Add Ref/Context7 mandate for documentation verification
- [ ] Ensure consistency with executor/planner changes
- [ ] Test workflows still function correctly

## Notes

This is a deferred todo per CONTEXT.md - minimum requirement was executor + planner.
```
  </action>
  <verify>
- `grep -c "Serena" .claude/get-shit-done/agents/planner.md` returns > 0
- `ls .planning/todos/pending/2026-01-21-gsd-workflow-updates.md` shows file exists
  </verify>
  <done>Planner updated with Serena/Ref mandate, todo created for remaining workflows</done>
</task>

</tasks>

<verification>
After all tasks:
1. Pattern docs exist: `.serena/memories/patterns/{svelte5,sveltekit,drizzle}-patterns.md`
2. Executor workflow has Serena/Ref section: `grep "Serena" .claude/get-shit-done/workflows/execute-plan.md`
3. Planner agent has Serena/Ref section: `grep "Serena" .claude/get-shit-done/agents/planner.md`
4. Todo exists for remaining workflows: `cat .planning/todos/pending/2026-01-21-gsd-workflow-updates.md`
</verification>

<success_criteria>
- Pattern documentation stored in Serena memories (3 files)
- GSD executor workflow mandates Serena/Ref usage
- GSD planner agent mandates Serena/Ref usage
- Todo created for remaining GSD workflow updates
- All changes preserve existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality-analysis/05-03-SUMMARY.md`
</output>
